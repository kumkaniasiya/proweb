services:
  - type: web
    name: boledi
    buildCommand: npm install && npm run build
    staticPublishPath: "./dist"
    headers:
      - path: "/*.js"
        name: "Content-Type"
        value: "text/javascript"
